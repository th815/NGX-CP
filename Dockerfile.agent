# 本地验证用镜像：基于已交叉编译的 linux/amd64 静态二进制。
# 注意：bin/ngxcp-agent 由 `make build` 生成（gitignore，不入库）。
FROM alpine:3.20
RUN mkdir -p /var/lib/ngxcp /etc/ngxcp
COPY bin/ngxcp-agent /opt/ngxcp/ngxcp-agent
RUN chmod +x /opt/ngxcp/ngxcp-agent
ENTRYPOINT ["/opt/ngxcp/ngxcp-agent"]
