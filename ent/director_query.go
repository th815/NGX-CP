// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/th/ngxcp/ent/director"
	"github.com/th/ngxcp/ent/node"
	"github.com/th/ngxcp/ent/predicate"
	"github.com/th/ngxcp/ent/virtualservice"
)

// DirectorQuery is the builder for querying Director entities.
type DirectorQuery struct {
	config
	ctx                 *QueryContext
	order               []director.OrderOption
	inters              []Interceptor
	predicates          []predicate.Director
	withNode            *NodeQuery
	withVirtualServices *VirtualServiceQuery
	withFKs             bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DirectorQuery builder.
func (_q *DirectorQuery) Where(ps ...predicate.Director) *DirectorQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *DirectorQuery) Limit(limit int) *DirectorQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *DirectorQuery) Offset(offset int) *DirectorQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *DirectorQuery) Unique(unique bool) *DirectorQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *DirectorQuery) Order(o ...director.OrderOption) *DirectorQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryNode chains the current query on the "node" edge.
func (_q *DirectorQuery) QueryNode() *NodeQuery {
	query := (&NodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(director.Table, director.FieldID, selector),
			sqlgraph.To(node.Table, node.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, director.NodeTable, director.NodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVirtualServices chains the current query on the "virtual_services" edge.
func (_q *DirectorQuery) QueryVirtualServices() *VirtualServiceQuery {
	query := (&VirtualServiceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(director.Table, director.FieldID, selector),
			sqlgraph.To(virtualservice.Table, virtualservice.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, director.VirtualServicesTable, director.VirtualServicesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Director entity from the query.
// Returns a *NotFoundError when no Director was found.
func (_q *DirectorQuery) First(ctx context.Context) (*Director, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{director.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *DirectorQuery) FirstX(ctx context.Context) *Director {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Director ID from the query.
// Returns a *NotFoundError when no Director ID was found.
func (_q *DirectorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{director.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *DirectorQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Director entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Director entity is found.
// Returns a *NotFoundError when no Director entities are found.
func (_q *DirectorQuery) Only(ctx context.Context) (*Director, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{director.Label}
	default:
		return nil, &NotSingularError{director.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *DirectorQuery) OnlyX(ctx context.Context) *Director {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Director ID in the query.
// Returns a *NotSingularError when more than one Director ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *DirectorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{director.Label}
	default:
		err = &NotSingularError{director.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *DirectorQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Directors.
func (_q *DirectorQuery) All(ctx context.Context) ([]*Director, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Director, *DirectorQuery]()
	return withInterceptors[[]*Director](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *DirectorQuery) AllX(ctx context.Context) []*Director {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Director IDs.
func (_q *DirectorQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(director.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *DirectorQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *DirectorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*DirectorQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *DirectorQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *DirectorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *DirectorQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DirectorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *DirectorQuery) Clone() *DirectorQuery {
	if _q == nil {
		return nil
	}
	return &DirectorQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]director.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.Director{}, _q.predicates...),
		withNode:            _q.withNode.Clone(),
		withVirtualServices: _q.withVirtualServices.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithNode tells the query-builder to eager-load the nodes that are connected to
// the "node" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DirectorQuery) WithNode(opts ...func(*NodeQuery)) *DirectorQuery {
	query := (&NodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNode = query
	return _q
}

// WithVirtualServices tells the query-builder to eager-load the nodes that are connected to
// the "virtual_services" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DirectorQuery) WithVirtualServices(opts ...func(*VirtualServiceQuery)) *DirectorQuery {
	query := (&VirtualServiceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVirtualServices = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		State director.State `json:"state,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Director.Query().
//		GroupBy(director.FieldState).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *DirectorQuery) GroupBy(field string, fields ...string) *DirectorGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DirectorGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = director.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		State director.State `json:"state,omitempty"`
//	}
//
//	client.Director.Query().
//		Select(director.FieldState).
//		Scan(ctx, &v)
func (_q *DirectorQuery) Select(fields ...string) *DirectorSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &DirectorSelect{DirectorQuery: _q}
	sbuild.label = director.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DirectorSelect configured with the given aggregations.
func (_q *DirectorQuery) Aggregate(fns ...AggregateFunc) *DirectorSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *DirectorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !director.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *DirectorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Director, error) {
	var (
		nodes       = []*Director{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withNode != nil,
			_q.withVirtualServices != nil,
		}
	)
	if _q.withNode != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, director.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Director).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Director{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withNode; query != nil {
		if err := _q.loadNode(ctx, query, nodes, nil,
			func(n *Director, e *Node) { n.Edges.Node = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVirtualServices; query != nil {
		if err := _q.loadVirtualServices(ctx, query, nodes,
			func(n *Director) { n.Edges.VirtualServices = []*VirtualService{} },
			func(n *Director, e *VirtualService) { n.Edges.VirtualServices = append(n.Edges.VirtualServices, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *DirectorQuery) loadNode(ctx context.Context, query *NodeQuery, nodes []*Director, init func(*Director), assign func(*Director, *Node)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Director)
	for i := range nodes {
		if nodes[i].node_directors == nil {
			continue
		}
		fk := *nodes[i].node_directors
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(node.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "node_directors" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *DirectorQuery) loadVirtualServices(ctx context.Context, query *VirtualServiceQuery, nodes []*Director, init func(*Director), assign func(*Director, *VirtualService)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Director)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.VirtualService(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(director.VirtualServicesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.director_virtual_services
		if fk == nil {
			return fmt.Errorf(`foreign-key "director_virtual_services" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "director_virtual_services" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *DirectorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *DirectorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(director.Table, director.Columns, sqlgraph.NewFieldSpec(director.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, director.FieldID)
		for i := range fields {
			if fields[i] != director.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *DirectorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(director.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = director.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DirectorGroupBy is the group-by builder for Director entities.
type DirectorGroupBy struct {
	selector
	build *DirectorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *DirectorGroupBy) Aggregate(fns ...AggregateFunc) *DirectorGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *DirectorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectorQuery, *DirectorGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *DirectorGroupBy) sqlScan(ctx context.Context, root *DirectorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DirectorSelect is the builder for selecting fields of Director entities.
type DirectorSelect struct {
	*DirectorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *DirectorSelect) Aggregate(fns ...AggregateFunc) *DirectorSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *DirectorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectorQuery, *DirectorSelect](ctx, _s.DirectorQuery, _s, _s.inters, v)
}

func (_s *DirectorSelect) sqlScan(ctx context.Context, root *DirectorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
