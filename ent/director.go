// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/th/ngxcp/ent/director"
	"github.com/th/ngxcp/ent/node"
)

// Director is the model entity for the Director schema.
type Director struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// VRRP 状态；双机一主一备
	State director.State `json:"state,omitempty"`
	// VRRP 优先级；MASTER 高（如 150）、BACKUP 低（如 100）
	Priority int `json:"priority,omitempty"`
	// virtual_router_id，同二层必须唯一（0-255）
	VirtualRouterID int `json:"virtual_router_id,omitempty"`
	// VRRP unicast 源地址（云环境禁组播，必须 unicast）
	UnicastSrcIP string `json:"unicast_src_ip,omitempty"`
	// 对端 Director 的 unicast 地址
	UnicastPeerIP string `json:"unicast_peer_ip,omitempty"`
	// 承载 VRRP 的物理网卡
	Iface string `json:"iface,omitempty"`
	// LVS 转发模式；本项目为 DR
	Mode director.Mode `json:"mode,omitempty"`
	// 该 Director 持有的虚拟 IP（lo:0，/32）
	Vip string `json:"vip,omitempty"`
	// 实时：当前是否持有 VIP（主备切换判据，T056）
	HoldingVip bool `json:"holding_vip,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DirectorQuery when eager-loading is set.
	Edges          DirectorEdges `json:"edges"`
	node_directors *int
	selectValues   sql.SelectValues
}

// DirectorEdges holds the relations/edges for other nodes in the graph.
type DirectorEdges struct {
	// Node holds the value of the node edge.
	Node *Node `json:"node,omitempty"`
	// VirtualServices holds the value of the virtual_services edge.
	VirtualServices []*VirtualService `json:"virtual_services,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// NodeOrErr returns the Node value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DirectorEdges) NodeOrErr() (*Node, error) {
	if e.Node != nil {
		return e.Node, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: node.Label}
	}
	return nil, &NotLoadedError{edge: "node"}
}

// VirtualServicesOrErr returns the VirtualServices value or an error if the edge
// was not loaded in eager-loading.
func (e DirectorEdges) VirtualServicesOrErr() ([]*VirtualService, error) {
	if e.loadedTypes[1] {
		return e.VirtualServices, nil
	}
	return nil, &NotLoadedError{edge: "virtual_services"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Director) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case director.FieldHoldingVip:
			values[i] = new(sql.NullBool)
		case director.FieldID, director.FieldPriority, director.FieldVirtualRouterID:
			values[i] = new(sql.NullInt64)
		case director.FieldState, director.FieldUnicastSrcIP, director.FieldUnicastPeerIP, director.FieldIface, director.FieldMode, director.FieldVip:
			values[i] = new(sql.NullString)
		case director.FieldCreatedAt, director.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case director.ForeignKeys[0]: // node_directors
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Director fields.
func (_m *Director) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case director.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case director.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = director.State(value.String)
			}
		case director.FieldPriority:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field priority", values[i])
			} else if value.Valid {
				_m.Priority = int(value.Int64)
			}
		case director.FieldVirtualRouterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field virtual_router_id", values[i])
			} else if value.Valid {
				_m.VirtualRouterID = int(value.Int64)
			}
		case director.FieldUnicastSrcIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field unicast_src_ip", values[i])
			} else if value.Valid {
				_m.UnicastSrcIP = value.String
			}
		case director.FieldUnicastPeerIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field unicast_peer_ip", values[i])
			} else if value.Valid {
				_m.UnicastPeerIP = value.String
			}
		case director.FieldIface:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field iface", values[i])
			} else if value.Valid {
				_m.Iface = value.String
			}
		case director.FieldMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mode", values[i])
			} else if value.Valid {
				_m.Mode = director.Mode(value.String)
			}
		case director.FieldVip:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vip", values[i])
			} else if value.Valid {
				_m.Vip = value.String
			}
		case director.FieldHoldingVip:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field holding_vip", values[i])
			} else if value.Valid {
				_m.HoldingVip = value.Bool
			}
		case director.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case director.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case director.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field node_directors", value)
			} else if value.Valid {
				_m.node_directors = new(int)
				*_m.node_directors = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Director.
// This includes values selected through modifiers, order, etc.
func (_m *Director) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryNode queries the "node" edge of the Director entity.
func (_m *Director) QueryNode() *NodeQuery {
	return NewDirectorClient(_m.config).QueryNode(_m)
}

// QueryVirtualServices queries the "virtual_services" edge of the Director entity.
func (_m *Director) QueryVirtualServices() *VirtualServiceQuery {
	return NewDirectorClient(_m.config).QueryVirtualServices(_m)
}

// Update returns a builder for updating this Director.
// Note that you need to call Director.Unwrap() before calling this method if this Director
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Director) Update() *DirectorUpdateOne {
	return NewDirectorClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Director entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Director) Unwrap() *Director {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Director is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Director) String() string {
	var builder strings.Builder
	builder.WriteString("Director(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("state=")
	builder.WriteString(fmt.Sprintf("%v", _m.State))
	builder.WriteString(", ")
	builder.WriteString("priority=")
	builder.WriteString(fmt.Sprintf("%v", _m.Priority))
	builder.WriteString(", ")
	builder.WriteString("virtual_router_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.VirtualRouterID))
	builder.WriteString(", ")
	builder.WriteString("unicast_src_ip=")
	builder.WriteString(_m.UnicastSrcIP)
	builder.WriteString(", ")
	builder.WriteString("unicast_peer_ip=")
	builder.WriteString(_m.UnicastPeerIP)
	builder.WriteString(", ")
	builder.WriteString("iface=")
	builder.WriteString(_m.Iface)
	builder.WriteString(", ")
	builder.WriteString("mode=")
	builder.WriteString(fmt.Sprintf("%v", _m.Mode))
	builder.WriteString(", ")
	builder.WriteString("vip=")
	builder.WriteString(_m.Vip)
	builder.WriteString(", ")
	builder.WriteString("holding_vip=")
	builder.WriteString(fmt.Sprintf("%v", _m.HoldingVip))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Directors is a parsable slice of Director.
type Directors []*Director
