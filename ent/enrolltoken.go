// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/th/ngxcp/ent/enrolltoken"
	"github.com/th/ngxcp/ent/node"
)

// EnrollToken is the model entity for the EnrollToken schema.
type EnrollToken struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SHA-256(原始令牌)，唯一；库内不存明文
	TokenHash string `json:"token_hash,omitempty"`
	// 令牌过期时间；过期即拒绝
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// 一次性使用标志；true 即失效（不可复用）
	Used bool `json:"used,omitempty"`
	// 吊销标志；true 即失效（预留：后期授权/商业化可据节点数策略吊销）
	Revoked bool `json:"revoked,omitempty"`
	// 使用时间，供审计
	UsedAt time.Time `json:"used_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EnrollTokenQuery when eager-loading is set.
	Edges              EnrollTokenEdges `json:"edges"`
	node_enroll_tokens *int
	selectValues       sql.SelectValues
}

// EnrollTokenEdges holds the relations/edges for other nodes in the graph.
type EnrollTokenEdges struct {
	// 令牌绑定的节点（1 token = 1 node）
	Node *Node `json:"node,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// NodeOrErr returns the Node value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EnrollTokenEdges) NodeOrErr() (*Node, error) {
	if e.Node != nil {
		return e.Node, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: node.Label}
	}
	return nil, &NotLoadedError{edge: "node"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EnrollToken) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case enrolltoken.FieldUsed, enrolltoken.FieldRevoked:
			values[i] = new(sql.NullBool)
		case enrolltoken.FieldID:
			values[i] = new(sql.NullInt64)
		case enrolltoken.FieldTokenHash:
			values[i] = new(sql.NullString)
		case enrolltoken.FieldExpiresAt, enrolltoken.FieldUsedAt, enrolltoken.FieldCreatedAt, enrolltoken.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case enrolltoken.ForeignKeys[0]: // node_enroll_tokens
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EnrollToken fields.
func (_m *EnrollToken) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case enrolltoken.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case enrolltoken.FieldTokenHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token_hash", values[i])
			} else if value.Valid {
				_m.TokenHash = value.String
			}
		case enrolltoken.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case enrolltoken.FieldUsed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field used", values[i])
			} else if value.Valid {
				_m.Used = value.Bool
			}
		case enrolltoken.FieldRevoked:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field revoked", values[i])
			} else if value.Valid {
				_m.Revoked = value.Bool
			}
		case enrolltoken.FieldUsedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field used_at", values[i])
			} else if value.Valid {
				_m.UsedAt = value.Time
			}
		case enrolltoken.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case enrolltoken.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case enrolltoken.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field node_enroll_tokens", value)
			} else if value.Valid {
				_m.node_enroll_tokens = new(int)
				*_m.node_enroll_tokens = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EnrollToken.
// This includes values selected through modifiers, order, etc.
func (_m *EnrollToken) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryNode queries the "node" edge of the EnrollToken entity.
func (_m *EnrollToken) QueryNode() *NodeQuery {
	return NewEnrollTokenClient(_m.config).QueryNode(_m)
}

// Update returns a builder for updating this EnrollToken.
// Note that you need to call EnrollToken.Unwrap() before calling this method if this EnrollToken
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EnrollToken) Update() *EnrollTokenUpdateOne {
	return NewEnrollTokenClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EnrollToken entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EnrollToken) Unwrap() *EnrollToken {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EnrollToken is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EnrollToken) String() string {
	var builder strings.Builder
	builder.WriteString("EnrollToken(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token_hash=")
	builder.WriteString(_m.TokenHash)
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("used=")
	builder.WriteString(fmt.Sprintf("%v", _m.Used))
	builder.WriteString(", ")
	builder.WriteString("revoked=")
	builder.WriteString(fmt.Sprintf("%v", _m.Revoked))
	builder.WriteString(", ")
	builder.WriteString("used_at=")
	builder.WriteString(_m.UsedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EnrollTokens is a parsable slice of EnrollToken.
type EnrollTokens []*EnrollToken
