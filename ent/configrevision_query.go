// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/th/ngxcp/ent/configblob"
	"github.com/th/ngxcp/ent/configfile"
	"github.com/th/ngxcp/ent/configrevision"
	"github.com/th/ngxcp/ent/predicate"
)

// ConfigRevisionQuery is the builder for querying ConfigRevision entities.
type ConfigRevisionQuery struct {
	config
	ctx            *QueryContext
	order          []configrevision.OrderOption
	inters         []Interceptor
	predicates     []predicate.ConfigRevision
	withBlob       *ConfigBlobQuery
	withParent     *ConfigRevisionQuery
	withChildren   *ConfigRevisionQuery
	withConfigFile *ConfigFileQuery
	withFKs        bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ConfigRevisionQuery builder.
func (_q *ConfigRevisionQuery) Where(ps ...predicate.ConfigRevision) *ConfigRevisionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ConfigRevisionQuery) Limit(limit int) *ConfigRevisionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ConfigRevisionQuery) Offset(offset int) *ConfigRevisionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ConfigRevisionQuery) Unique(unique bool) *ConfigRevisionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ConfigRevisionQuery) Order(o ...configrevision.OrderOption) *ConfigRevisionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBlob chains the current query on the "blob" edge.
func (_q *ConfigRevisionQuery) QueryBlob() *ConfigBlobQuery {
	query := (&ConfigBlobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(configrevision.Table, configrevision.FieldID, selector),
			sqlgraph.To(configblob.Table, configblob.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, configrevision.BlobTable, configrevision.BlobColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParent chains the current query on the "parent" edge.
func (_q *ConfigRevisionQuery) QueryParent() *ConfigRevisionQuery {
	query := (&ConfigRevisionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(configrevision.Table, configrevision.FieldID, selector),
			sqlgraph.To(configrevision.Table, configrevision.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, configrevision.ParentTable, configrevision.ParentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildren chains the current query on the "children" edge.
func (_q *ConfigRevisionQuery) QueryChildren() *ConfigRevisionQuery {
	query := (&ConfigRevisionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(configrevision.Table, configrevision.FieldID, selector),
			sqlgraph.To(configrevision.Table, configrevision.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, configrevision.ChildrenTable, configrevision.ChildrenColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryConfigFile chains the current query on the "config_file" edge.
func (_q *ConfigRevisionQuery) QueryConfigFile() *ConfigFileQuery {
	query := (&ConfigFileClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(configrevision.Table, configrevision.FieldID, selector),
			sqlgraph.To(configfile.Table, configfile.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, configrevision.ConfigFileTable, configrevision.ConfigFileColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ConfigRevision entity from the query.
// Returns a *NotFoundError when no ConfigRevision was found.
func (_q *ConfigRevisionQuery) First(ctx context.Context) (*ConfigRevision, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{configrevision.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ConfigRevisionQuery) FirstX(ctx context.Context) *ConfigRevision {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ConfigRevision ID from the query.
// Returns a *NotFoundError when no ConfigRevision ID was found.
func (_q *ConfigRevisionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{configrevision.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ConfigRevisionQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ConfigRevision entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ConfigRevision entity is found.
// Returns a *NotFoundError when no ConfigRevision entities are found.
func (_q *ConfigRevisionQuery) Only(ctx context.Context) (*ConfigRevision, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{configrevision.Label}
	default:
		return nil, &NotSingularError{configrevision.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ConfigRevisionQuery) OnlyX(ctx context.Context) *ConfigRevision {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ConfigRevision ID in the query.
// Returns a *NotSingularError when more than one ConfigRevision ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ConfigRevisionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{configrevision.Label}
	default:
		err = &NotSingularError{configrevision.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ConfigRevisionQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ConfigRevisions.
func (_q *ConfigRevisionQuery) All(ctx context.Context) ([]*ConfigRevision, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ConfigRevision, *ConfigRevisionQuery]()
	return withInterceptors[[]*ConfigRevision](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ConfigRevisionQuery) AllX(ctx context.Context) []*ConfigRevision {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ConfigRevision IDs.
func (_q *ConfigRevisionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(configrevision.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ConfigRevisionQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ConfigRevisionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ConfigRevisionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ConfigRevisionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ConfigRevisionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ConfigRevisionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ConfigRevisionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ConfigRevisionQuery) Clone() *ConfigRevisionQuery {
	if _q == nil {
		return nil
	}
	return &ConfigRevisionQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]configrevision.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.ConfigRevision{}, _q.predicates...),
		withBlob:       _q.withBlob.Clone(),
		withParent:     _q.withParent.Clone(),
		withChildren:   _q.withChildren.Clone(),
		withConfigFile: _q.withConfigFile.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithBlob tells the query-builder to eager-load the nodes that are connected to
// the "blob" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ConfigRevisionQuery) WithBlob(opts ...func(*ConfigBlobQuery)) *ConfigRevisionQuery {
	query := (&ConfigBlobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBlob = query
	return _q
}

// WithParent tells the query-builder to eager-load the nodes that are connected to
// the "parent" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ConfigRevisionQuery) WithParent(opts ...func(*ConfigRevisionQuery)) *ConfigRevisionQuery {
	query := (&ConfigRevisionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParent = query
	return _q
}

// WithChildren tells the query-builder to eager-load the nodes that are connected to
// the "children" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ConfigRevisionQuery) WithChildren(opts ...func(*ConfigRevisionQuery)) *ConfigRevisionQuery {
	query := (&ConfigRevisionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChildren = query
	return _q
}

// WithConfigFile tells the query-builder to eager-load the nodes that are connected to
// the "config_file" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ConfigRevisionQuery) WithConfigFile(opts ...func(*ConfigFileQuery)) *ConfigRevisionQuery {
	query := (&ConfigFileClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withConfigFile = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NodeID int `json:"node_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ConfigRevision.Query().
//		GroupBy(configrevision.FieldNodeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ConfigRevisionQuery) GroupBy(field string, fields ...string) *ConfigRevisionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ConfigRevisionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = configrevision.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NodeID int `json:"node_id,omitempty"`
//	}
//
//	client.ConfigRevision.Query().
//		Select(configrevision.FieldNodeID).
//		Scan(ctx, &v)
func (_q *ConfigRevisionQuery) Select(fields ...string) *ConfigRevisionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ConfigRevisionSelect{ConfigRevisionQuery: _q}
	sbuild.label = configrevision.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ConfigRevisionSelect configured with the given aggregations.
func (_q *ConfigRevisionQuery) Aggregate(fns ...AggregateFunc) *ConfigRevisionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ConfigRevisionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !configrevision.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ConfigRevisionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ConfigRevision, error) {
	var (
		nodes       = []*ConfigRevision{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withBlob != nil,
			_q.withParent != nil,
			_q.withChildren != nil,
			_q.withConfigFile != nil,
		}
	)
	if _q.withBlob != nil || _q.withParent != nil || _q.withConfigFile != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, configrevision.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ConfigRevision).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ConfigRevision{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBlob; query != nil {
		if err := _q.loadBlob(ctx, query, nodes, nil,
			func(n *ConfigRevision, e *ConfigBlob) { n.Edges.Blob = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParent; query != nil {
		if err := _q.loadParent(ctx, query, nodes, nil,
			func(n *ConfigRevision, e *ConfigRevision) { n.Edges.Parent = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChildren; query != nil {
		if err := _q.loadChildren(ctx, query, nodes,
			func(n *ConfigRevision) { n.Edges.Children = []*ConfigRevision{} },
			func(n *ConfigRevision, e *ConfigRevision) { n.Edges.Children = append(n.Edges.Children, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withConfigFile; query != nil {
		if err := _q.loadConfigFile(ctx, query, nodes, nil,
			func(n *ConfigRevision, e *ConfigFile) { n.Edges.ConfigFile = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ConfigRevisionQuery) loadBlob(ctx context.Context, query *ConfigBlobQuery, nodes []*ConfigRevision, init func(*ConfigRevision), assign func(*ConfigRevision, *ConfigBlob)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ConfigRevision)
	for i := range nodes {
		if nodes[i].config_blob_revisions == nil {
			continue
		}
		fk := *nodes[i].config_blob_revisions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(configblob.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "config_blob_revisions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ConfigRevisionQuery) loadParent(ctx context.Context, query *ConfigRevisionQuery, nodes []*ConfigRevision, init func(*ConfigRevision), assign func(*ConfigRevision, *ConfigRevision)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ConfigRevision)
	for i := range nodes {
		if nodes[i].config_revision_children == nil {
			continue
		}
		fk := *nodes[i].config_revision_children
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(configrevision.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "config_revision_children" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ConfigRevisionQuery) loadChildren(ctx context.Context, query *ConfigRevisionQuery, nodes []*ConfigRevision, init func(*ConfigRevision), assign func(*ConfigRevision, *ConfigRevision)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ConfigRevision)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ConfigRevision(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(configrevision.ChildrenColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.config_revision_children
		if fk == nil {
			return fmt.Errorf(`foreign-key "config_revision_children" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "config_revision_children" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ConfigRevisionQuery) loadConfigFile(ctx context.Context, query *ConfigFileQuery, nodes []*ConfigRevision, init func(*ConfigRevision), assign func(*ConfigRevision, *ConfigFile)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ConfigRevision)
	for i := range nodes {
		if nodes[i].config_file_current_revision == nil {
			continue
		}
		fk := *nodes[i].config_file_current_revision
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(configfile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "config_file_current_revision" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ConfigRevisionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ConfigRevisionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(configrevision.Table, configrevision.Columns, sqlgraph.NewFieldSpec(configrevision.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, configrevision.FieldID)
		for i := range fields {
			if fields[i] != configrevision.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ConfigRevisionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(configrevision.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = configrevision.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ConfigRevisionGroupBy is the group-by builder for ConfigRevision entities.
type ConfigRevisionGroupBy struct {
	selector
	build *ConfigRevisionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ConfigRevisionGroupBy) Aggregate(fns ...AggregateFunc) *ConfigRevisionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ConfigRevisionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ConfigRevisionQuery, *ConfigRevisionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ConfigRevisionGroupBy) sqlScan(ctx context.Context, root *ConfigRevisionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ConfigRevisionSelect is the builder for selecting fields of ConfigRevision entities.
type ConfigRevisionSelect struct {
	*ConfigRevisionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ConfigRevisionSelect) Aggregate(fns ...AggregateFunc) *ConfigRevisionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ConfigRevisionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ConfigRevisionQuery, *ConfigRevisionSelect](ctx, _s.ConfigRevisionQuery, _s, _s.inters, v)
}

func (_s *ConfigRevisionSelect) sqlScan(ctx context.Context, root *ConfigRevisionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
