// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/th/ngxcp/ent/certdeployment"
	"github.com/th/ngxcp/ent/certificate"
)

// CertDeployment is the model entity for the CertDeployment schema.
type CertDeployment struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 目标节点 ID
	NodeID int `json:"node_id,omitempty"`
	// 分发状态
	Status certdeployment.Status `json:"status,omitempty"`
	// 实际落盘时间（UTC）
	DeployedAt time.Time `json:"deployed_at,omitempty"`
	// 失败原因（人类可读）
	Error string `json:"error,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CertDeploymentQuery when eager-loading is set.
	Edges                   CertDeploymentEdges `json:"edges"`
	certificate_deployments *int
	selectValues            sql.SelectValues
}

// CertDeploymentEdges holds the relations/edges for other nodes in the graph.
type CertDeploymentEdges struct {
	// Certificate holds the value of the certificate edge.
	Certificate *Certificate `json:"certificate,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CertificateOrErr returns the Certificate value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CertDeploymentEdges) CertificateOrErr() (*Certificate, error) {
	if e.Certificate != nil {
		return e.Certificate, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: certificate.Label}
	}
	return nil, &NotLoadedError{edge: "certificate"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CertDeployment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case certdeployment.FieldID, certdeployment.FieldNodeID:
			values[i] = new(sql.NullInt64)
		case certdeployment.FieldStatus, certdeployment.FieldError:
			values[i] = new(sql.NullString)
		case certdeployment.FieldDeployedAt:
			values[i] = new(sql.NullTime)
		case certdeployment.ForeignKeys[0]: // certificate_deployments
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CertDeployment fields.
func (_m *CertDeployment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case certdeployment.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case certdeployment.FieldNodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field node_id", values[i])
			} else if value.Valid {
				_m.NodeID = int(value.Int64)
			}
		case certdeployment.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = certdeployment.Status(value.String)
			}
		case certdeployment.FieldDeployedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deployed_at", values[i])
			} else if value.Valid {
				_m.DeployedAt = value.Time
			}
		case certdeployment.FieldError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error", values[i])
			} else if value.Valid {
				_m.Error = value.String
			}
		case certdeployment.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field certificate_deployments", value)
			} else if value.Valid {
				_m.certificate_deployments = new(int)
				*_m.certificate_deployments = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CertDeployment.
// This includes values selected through modifiers, order, etc.
func (_m *CertDeployment) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCertificate queries the "certificate" edge of the CertDeployment entity.
func (_m *CertDeployment) QueryCertificate() *CertificateQuery {
	return NewCertDeploymentClient(_m.config).QueryCertificate(_m)
}

// Update returns a builder for updating this CertDeployment.
// Note that you need to call CertDeployment.Unwrap() before calling this method if this CertDeployment
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CertDeployment) Update() *CertDeploymentUpdateOne {
	return NewCertDeploymentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CertDeployment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CertDeployment) Unwrap() *CertDeployment {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CertDeployment is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CertDeployment) String() string {
	var builder strings.Builder
	builder.WriteString("CertDeployment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("node_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.NodeID))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("deployed_at=")
	builder.WriteString(_m.DeployedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("error=")
	builder.WriteString(_m.Error)
	builder.WriteByte(')')
	return builder.String()
}

// CertDeployments is a parsable slice of CertDeployment.
type CertDeployments []*CertDeployment
