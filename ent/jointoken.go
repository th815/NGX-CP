// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/th/ngxcp/ent/jointoken"
	"github.com/th/ngxcp/ent/node"
)

// JoinToken is the model entity for the JoinToken schema.
type JoinToken struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SHA-256(原始令牌)，唯一；库内不存明文
	TokenHash string `json:"token_hash,omitempty"`
	// 签发时锁定的节点角色
	Role jointoken.Role `json:"role,omitempty"`
	// 令牌过期时间；enrolling 阶段过期即拒绝，已纳管节点不受影响
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// 吊销标志；true 即失效（旋转/泄漏时立即吊销）
	Revoked bool `json:"revoked,omitempty"`
	// 最近一次成功用于注册的时间，供审计/异常检测
	LastUsedAt time.Time `json:"last_used_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JoinTokenQuery when eager-loading is set.
	Edges            JoinTokenEdges `json:"edges"`
	node_join_tokens *int
	selectValues     sql.SelectValues
}

// JoinTokenEdges holds the relations/edges for other nodes in the graph.
type JoinTokenEdges struct {
	// 令牌绑定的节点（1 token = 1 node）
	Node *Node `json:"node,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// NodeOrErr returns the Node value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JoinTokenEdges) NodeOrErr() (*Node, error) {
	if e.Node != nil {
		return e.Node, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: node.Label}
	}
	return nil, &NotLoadedError{edge: "node"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JoinToken) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case jointoken.FieldRevoked:
			values[i] = new(sql.NullBool)
		case jointoken.FieldID:
			values[i] = new(sql.NullInt64)
		case jointoken.FieldTokenHash, jointoken.FieldRole:
			values[i] = new(sql.NullString)
		case jointoken.FieldExpiresAt, jointoken.FieldLastUsedAt, jointoken.FieldCreatedAt, jointoken.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case jointoken.ForeignKeys[0]: // node_join_tokens
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JoinToken fields.
func (_m *JoinToken) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jointoken.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case jointoken.FieldTokenHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token_hash", values[i])
			} else if value.Valid {
				_m.TokenHash = value.String
			}
		case jointoken.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = jointoken.Role(value.String)
			}
		case jointoken.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case jointoken.FieldRevoked:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field revoked", values[i])
			} else if value.Valid {
				_m.Revoked = value.Bool
			}
		case jointoken.FieldLastUsedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_used_at", values[i])
			} else if value.Valid {
				_m.LastUsedAt = value.Time
			}
		case jointoken.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case jointoken.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case jointoken.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field node_join_tokens", value)
			} else if value.Valid {
				_m.node_join_tokens = new(int)
				*_m.node_join_tokens = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JoinToken.
// This includes values selected through modifiers, order, etc.
func (_m *JoinToken) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryNode queries the "node" edge of the JoinToken entity.
func (_m *JoinToken) QueryNode() *NodeQuery {
	return NewJoinTokenClient(_m.config).QueryNode(_m)
}

// Update returns a builder for updating this JoinToken.
// Note that you need to call JoinToken.Unwrap() before calling this method if this JoinToken
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *JoinToken) Update() *JoinTokenUpdateOne {
	return NewJoinTokenClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the JoinToken entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *JoinToken) Unwrap() *JoinToken {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: JoinToken is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *JoinToken) String() string {
	var builder strings.Builder
	builder.WriteString("JoinToken(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token_hash=")
	builder.WriteString(_m.TokenHash)
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(fmt.Sprintf("%v", _m.Role))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("revoked=")
	builder.WriteString(fmt.Sprintf("%v", _m.Revoked))
	builder.WriteString(", ")
	builder.WriteString("last_used_at=")
	builder.WriteString(_m.LastUsedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JoinTokens is a parsable slice of JoinToken.
type JoinTokens []*JoinToken
